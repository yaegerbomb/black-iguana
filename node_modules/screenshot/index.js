var cp = require("child_process");
const ipcMain = require('electron').ipcMain;

module.exports = {
	snap: function(){
		cp.exec(__dirname + '/createscreenshot.exe "' + __dirname + '/../images""', function(err, data) { 		
		});
	}
}